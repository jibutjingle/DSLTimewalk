using UnityEngine;

public class PlacementZone : MonoBehaviour
{
    //[Header("Where objects should snap to")]
   // public Transform snapTarget; // Assign in Inspector

 //   private void OnTriggerEnter(Collider other)
  //  {
   //     ObjectPlacer placer = other.GetComponent<ObjectPlacer>();
    ////    if (placer != null)
      //  {
      //      placer.SetSnapTarget(snapTarget.gameObject);
      //      Debug.Log($"Snap target set for {other.name}");
      //  }
  //  }
}
